name: Demo
on:
  push: {}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build library
        run: make
      - name: Run tests
        run: make run_tests 1>/dev/null
