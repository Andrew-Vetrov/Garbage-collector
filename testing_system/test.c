#include "../scanner/marking.h"
#include "../allocator/allocator.h"
#include <stdio.h>

int main() {
	//A* a[1000];
	for (int i = 0; i < 100; i++) {
		//a[i] = (A*)allocate_new_object(16);
	}
	return 0;
}
